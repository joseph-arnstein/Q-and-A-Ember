<h1>Question</h1>
<!-- <h2>{{model.title}}</h2> -->

{{full-post post=model update="update" destroyPost="destroyPost"}}

{{new-comment saveComment2="saveComment3" post=model}}

<ul>
  {{#each model.comments as |comment|}}
    <li>
      <!-- {{comment.post.title}}
      <br> -->
      {{comment.complaint}}
      <br>
      Answer submittd by: {{comment.commentor}}
    </li>
    <button {{action 'deleteComment' comment}}>Delete this Comment</button>
  {{/each}}
</ul>
